.card{:class => "#{cookies.signed['user.id'] == message.user_id ? 'right-card' : 'left-card'}"}
  .card-block
    .card-text
      %span.date-text-muted
        = message.timestamp
        %br/
      = message.body
